# Firebase (V5 layout)

**Production config:** `/firebase-config.js` (repo root — required by GitHub Pages paths in `index.html` and `vedman-panel.html`).

**Legacy module:** archived to `archive/firebase-legacy/firebase.js` (admin only, SDK 10.8.0).

Do not move `firebase-config.js` without updating all panel and gallery script references.
